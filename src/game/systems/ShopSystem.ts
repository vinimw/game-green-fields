export type PurchaseResult={success:boolean;coins:number;baseHealth:number;message:string};
export const purchaseBaseHealth=(coins:number,currentHealth:number,maximumHealth:number,cost:number,healthRestore:number):PurchaseResult=>{if(currentHealth>=maximumHealth)return{success:false,coins,baseHealth:currentHealth,message:'Base health is already full'};if(coins<cost)return{success:false,coins,baseHealth:currentHealth,message:'Not enough coins'};return{success:true,coins:coins-cost,baseHealth:Math.min(maximumHealth,currentHealth+healthRestore),message:`Base restored by ${Math.min(healthRestore,maximumHealth-currentHealth)} HP`};};
