import { GAME_CONFIG } from '../config/gameConfig';import { getArcherWeapon,MAX_ARCHER_WEAPON_LEVEL } from '../config/archerWeaponsConfig';import type { PlayerState,SaveData } from '../core/types';
export class SaveSystem {hasSave(){return this.load()!==null;}save(data:SaveData){localStorage.setItem(GAME_CONFIG.saveKey,JSON.stringify(data));}clear(){localStorage.removeItem(GAME_CONFIG.saveKey);}load():SaveData|null{try{const raw=localStorage.getItem(GAME_CONFIG.saveKey);if(!raw)return null;const parsed:unknown=JSON.parse(raw);if(typeof parsed!=='object'||parsed===null||!('version'in parsed)||parsed.version!==1)return null;const data=parsed as SaveData,player=data.player;if(!('powerType'in player))(player as PlayerState).powerType=GAME_CONFIG.player.initialPowerType;if(!('coins'in player))(player as PlayerState).coins=GAME_CONFIG.player.initialCoins;if(!('archerWeaponLevel'in player))(player as PlayerState).archerWeaponLevel=1;player.archerWeaponLevel=Math.max(1,Math.min(MAX_ARCHER_WEAPON_LEVEL,player.archerWeaponLevel));if(player.powerType==='archer')player.equipment.weapon=getArcherWeapon(player.archerWeaponLevel)?.id??'training-bow';return data;}catch{return null;}}}
