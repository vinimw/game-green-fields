import { describe,expect,it } from 'vitest';import { purchaseBaseHealth } from './ShopSystem';
describe('shop',()=>{it('spends two coins and restores one hundred base health',()=>expect(purchaseBaseHealth(5,100,300,2,100)).toEqual({success:true,coins:3,baseHealth:200,message:'Base restored by 100 HP'}));it('clamps repair at maximum health',()=>expect(purchaseBaseHealth(2,250,300,2,100).baseHealth).toBe(300));it('rejects a purchase without enough coins',()=>expect(purchaseBaseHealth(1,100,300,2,100).success).toBe(false));it('does not spend coins when base is full',()=>expect(purchaseBaseHealth(2,300,300,2,100).coins).toBe(2));});
