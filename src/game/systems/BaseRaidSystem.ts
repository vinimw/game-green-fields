import { GAME_CONFIG } from '../config/gameConfig';import type { BaseState,Vec2 } from '../core/types';
export type RaidTransition='none'|'started'|'ended';
export class BaseRaidSystem {readonly position:Vec2={...GAME_CONFIG.base.position};state:BaseState;private playerLevel:number;constructor(saved?:BaseState,playerLevel=1){this.playerLevel=playerLevel;this.state=saved?{...saved}:{currentHealth:this.maxHealth,raidActive:false,remainingTimeMs:GAME_CONFIG.base.raidIntervalMs};this.state.currentHealth=Math.min(this.state.currentHealth,this.maxHealth);}get maxHealth(){return GAME_CONFIG.base.maxHealth*this.playerLevel;}syncPlayerLevel(level:number){if(level===this.playerLevel)return;const previousMaximum=this.maxHealth;this.playerLevel=level;this.state.currentHealth=Math.min(this.maxHealth,this.state.currentHealth+Math.max(0,this.maxHealth-previousMaximum));}update(deltaMs:number):RaidTransition{this.state.remainingTimeMs-=deltaMs;if(this.state.remainingTimeMs>0)return'none';if(this.state.raidActive){this.state.raidActive=false;this.state.remainingTimeMs=GAME_CONFIG.base.raidIntervalMs;return'ended';}this.state.raidActive=true;this.state.remainingTimeMs=GAME_CONFIG.base.raidDurationMs;return'started';}damage(amount:number){if(this.state.currentHealth<=0)return 0;const before=this.state.currentHealth;this.state.currentHealth=Math.max(0,before-amount);return before-this.state.currentHealth;}snapshot(){return{...this.state};}}
