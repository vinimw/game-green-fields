import { describe,expect,it } from 'vitest';import { createPlayerState } from '../core/GameState';import { attackRange } from './StatsSystem';import { isTargetInRange } from './CombatSystem';
describe('archer combat',()=>{it('starts with a bow equipped',()=>expect(createPlayerState('archer').equipment.weapon).toBe('training-bow'));it('has more range than magic',()=>expect(attackRange('archer')).toBeGreaterThan(attackRange('magic')));it('only accepts targets inside its radius',()=>{expect(isTargetInRange(attackRange('archer'),attackRange('archer'))).toBe(true);expect(isTargetInRange(attackRange('archer')+.1,attackRange('archer'))).toBe(false);});});
