import { describe,expect,it } from 'vitest';import { MONSTERS_CONFIG } from '../config/monstersConfig';import { addCoins,shouldDropCoins } from './CoinPickupSystem';
describe('coin drops',()=>{it('uses the configured chance and two coins for slime',()=>{const drop=MONSTERS_CONFIG.slime.coinDrop;expect(drop.amount).toBe(2);expect(drop.chance).toBeGreaterThan(0);expect(drop.chance).toBeLessThanOrEqual(1);expect(shouldDropCoins(drop.chance-.01,drop.chance)).toBe(true);expect(shouldDropCoins(drop.chance,drop.chance)).toBe(false);});it('uses a valid configured chance and five coins for sunflower',()=>{const drop=MONSTERS_CONFIG['evil-sunflower'].coinDrop;expect(drop.amount).toBe(5);expect(drop.chance).toBeGreaterThan(0);expect(drop.chance).toBeLessThanOrEqual(1);});it('adds collected coins to the total',()=>expect(addCoins(7,5)).toBe(12));});
