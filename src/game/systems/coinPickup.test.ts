import { describe,expect,it } from 'vitest';import { MONSTERS_CONFIG } from '../config/monstersConfig';import { addCoins,shouldDropCoins } from './CoinPickupSystem';describe('coin drops',()=>{it('uses Crawler drop configuration',()=>expect(MONSTERS_CONFIG.crawler.coinDrop).toEqual({chance:.35,amount:2}));it('uses Wailer drop configuration',()=>expect(MONSTERS_CONFIG.wailer.coinDrop).toEqual({chance:.5,amount:4}));it('respects exact chance boundary',()=>{const drop=MONSTERS_CONFIG.crawler.coinDrop;expect(shouldDropCoins(.34,drop.chance)).toBe(true);expect(shouldDropCoins(.35,drop.chance)).toBe(false);});it('adds collected coins to total',()=>expect(addCoins(7,5)).toBe(12));});
