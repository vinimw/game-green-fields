import { describe,expect,it } from 'vitest';import { MONSTERS_CONFIG } from '../config/monstersConfig';import { addCoins,shouldDropCoins } from './CoinPickupSystem';
describe('coin drops',()=>{it('uses 30% chance and two coins for slime',()=>{expect(MONSTERS_CONFIG.slime.coinDrop).toEqual({chance:.3,amount:2});expect(shouldDropCoins(.29,.3)).toBe(true);expect(shouldDropCoins(.3,.3)).toBe(false);});it('uses 40% chance and five coins for sunflower',()=>expect(MONSTERS_CONFIG['evil-sunflower'].coinDrop).toEqual({chance:.4,amount:5}));it('adds collected coins to the total',()=>expect(addCoins(7,5)).toBe(12));});
