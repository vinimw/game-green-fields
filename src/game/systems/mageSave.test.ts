import {beforeEach,describe,expect,it} from "vitest";import {GAME_CONFIG} from "../config/gameConfig";import {getStaff} from "../config/mageConfig";import {createPlayerState} from "../core/GameState";import type {SaveData} from "../core/types";import {SaveSystem} from "./SaveSystem";
describe("Mage save",()=>{const memory=new Map<string,string>();beforeEach(()=>Object.defineProperty(globalThis,"localStorage",{configurable:true,value:{getItem:(key:string)=>memory.get(key)??null,setItem:(key:string,value:string)=>memory.set(key,value),removeItem:(key:string)=>memory.delete(key)}}));it("preserves selected spell and Staff level",()=>{memory.clear();const player=createPlayerState("magic");player.selectedSpell="lightning-lance";player.staffLevel=12;player.equipment.weapon=getStaff(12).id;const data:SaveData={version:1,player,world:{mapId:GAME_CONFIG.mapId,monsters:[],npcs:[],objects:[]}};const saves=new SaveSystem();saves.save(data);expect(saves.load()?.player).toMatchObject({selectedSpell:"lightning-lance",staffLevel:12,equipment:{weapon:"heart-of-eternity"}});});});
