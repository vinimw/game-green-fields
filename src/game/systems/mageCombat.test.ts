import {describe,expect,it} from "vitest";import {MAGE_CONFIG} from "../config/mageConfig";import {rollMageDamage,targetsInsideRadius} from "./MageCombatSystem";
describe("Mage AoE combat",()=>{const targets=[{id:"primary",position:{x:0,z:0}},{id:"inside",position:{x:1,z:0}},{id:"outside",position:{x:4,z:0}}];it("finds primary and secondary targets only inside the Staff radius",()=>expect(targetsInsideRadius({x:0,z:0},targets,1.5).map(target=>target.id)).toEqual(["primary","inside"]));it("applies full damage and rolls critical independently per target",()=>{const rolls=[.99,0],random=()=>rolls.shift()??.99,normal=rollMageDamage(12,1,random),critical=rollMageDamage(12,1,random);expect(normal).toEqual({critical:false,damage:12});expect(critical).toEqual({critical:true,damage:12*MAGE_CONFIG.damage.baseDamage/MAGE_CONFIG.damage.baseDamage*2});});});
