import { describe,expect,it } from 'vitest';import { GAME_CONFIG } from '../config/gameConfig';import { BaseRaidSystem } from './BaseRaidSystem';
describe('base raids',()=>{it('starts after the configured interval',()=>{const raid=new BaseRaidSystem();expect(raid.update(GAME_CONFIG.base.raidIntervalMs)).toBe('started');});it('ends after the configured duration',()=>{const raid=new BaseRaidSystem();raid.update(GAME_CONFIG.base.raidIntervalMs);expect(raid.update(GAME_CONFIG.base.raidDurationMs)).toBe('ended');});it('cannot go below zero',()=>{const raid=new BaseRaidSystem();raid.damage(400);expect(raid.state.currentHealth).toBe(0);});it('multiplies base health by player level',()=>{const raid=new BaseRaidSystem(undefined,3);expect(raid.maxHealth).toBe(900);expect(raid.state.currentHealth).toBe(900);});it('preserves damage when maximum increases',()=>{const raid=new BaseRaidSystem();raid.damage(50);raid.syncPlayerLevel(2);expect(raid.maxHealth).toBe(600);expect(raid.state.currentHealth).toBe(550);});});
