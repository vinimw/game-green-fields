import { Color3,Mesh,MeshBuilder,StandardMaterial,TransformNode,Vector3,type Scene } from '@babylonjs/core';import type { MonsterState,Vec2 } from '../core/types';import { GAME_CONFIG } from '../config/gameConfig';import { MONSTERS_CONFIG } from '../config/monstersConfig';import type { MonsterNavigationSystem } from '../systems/MonsterNavigationSystem';import type { Player } from './Player';
export type MonsterAIState='IDLE'|'CHASING'|'ATTACKING'|'RETURNING'|'DEAD';
export class Monster {root:TransformNode;meshes:Mesh[]=[];aiState:MonsterAIState;readyForRemoval=false;private visual:TransformNode;private cooldown=0;private deathTime=0;private animationTime=Math.random()*10;private moving=false;private wanderTarget?:Vec2;private wanderWaitMs=Math.random()*1000;private navigation?:MonsterNavigationSystem;
constructor(private scene:Scene,public state:MonsterState){this.root=new TransformNode(state.id,scene);this.visual=new TransformNode(`${state.id}-visual`,scene);this.visual.parent=this.root;this.root.position.set(state.position.x,0,state.position.z);this.aiState=state.alive?'IDLE':'DEAD';this.build();if(!state.alive)this.root.setEnabled(false);}
private material(name:string,color:string){const result=new StandardMaterial(name,this.scene);result.diffuseColor=Color3.FromHexString(color);return result;}private add(mesh:Mesh,y:number,value:StandardMaterial){mesh.parent=this.visual;mesh.position.y=y;mesh.material=value;this.meshes.push(mesh);}
private build(){if(this.state.type==='crawler'){const flesh=this.material('crawler-flesh','#454A43'),bone=this.material('crawler-bone','#777568'),voidMaterial=this.material('crawler-eyes','#B8633D'),torso=MeshBuilder.CreateCapsule('crawler-torso',{height:1.65,radius:.38},this.scene);this.add(torso,.6,flesh);torso.rotation.x=Math.PI/2;torso.scaling.z=1.25;const head=MeshBuilder.CreateSphere('crawler-head',{diameter:.62,segments:10},this.scene);this.add(head,.65,bone);head.position.z=.75;head.scaling.y=.72;for(const x of [-.42,.42]){const arm=MeshBuilder.CreateCapsule('crawler-long-arm',{height:1.75,radius:.1},this.scene);arm.parent=this.visual;arm.material=flesh;arm.position.set(x,.45,.25);arm.rotation.z=x>0?-.75:.75;this.meshes.push(arm);const eye=MeshBuilder.CreateSphere('crawler-eye',{diameter:.08,segments:6},this.scene);eye.parent=this.visual;eye.material=voidMaterial;eye.position.set(x*.18,.7,1.03);this.meshes.push(eye);}}else{const cloth=this.material('wailer-cloth','#30363B'),skin=this.material('wailer-skin','#777A73'),face=this.material('wailer-void','#17191C'),body=MeshBuilder.CreateCapsule('wailer-body',{height:3.8,radius:.28},this.scene);this.add(body,1.9,cloth);body.scaling.x=.72;const head=MeshBuilder.CreateSphere('wailer-head',{diameter:.7,segments:10},this.scene);this.add(head,4,skin);head.scaling.y=1.45;const hollow=MeshBuilder.CreateSphere('wailer-face',{diameter:.32,segments:8},this.scene);hollow.parent=this.visual;hollow.material=face;hollow.position.set(0,4,.32);hollow.scaling.y=1.4;this.meshes.push(hollow);for(const x of [-.44,.44]){const arm=MeshBuilder.CreateCapsule('wailer-arm',{height:3.2,radius:.09},this.scene);arm.parent=this.visual;arm.material=skin;arm.position.set(x,2.1,0);arm.rotation.z=x>0?-.12:.12;this.meshes.push(arm);}}}
update(player:Player,dt:number,damagePlayer:(damage:number)=>void,paused:boolean,raid?:{active:boolean;position:Vec2;damageBase:(damage:number)=>void}){if(paused||this.aiState==='DEAD')return;this.moving=false;this.cooldown=Math.max(0,this.cooldown-dt*1000);this.wanderWaitMs=Math.max(0,this.wanderWaitMs-dt*1000);const config=MONSTERS_CONFIG[this.state.type];if(raid?.active){this.wanderTarget=undefined;const distance=Math.hypot(this.root.position.x-raid.position.x,this.root.position.z-raid.position.z);this.aiState=distance<=config.attackRadius?'ATTACKING':'CHASING';if(this.aiState==='CHASING')this.moveToward(raid.position.x,raid.position.z,config.movementSpeed,dt);if(this.aiState==='ATTACKING'&&this.cooldown===0){raid.damageBase(config.damage);this.cooldown=config.attackCooldownMs;}this.finishUpdate(dt);return;}const target=player.root.position,spawn=this.state.spawnPosition,playerDistance=Vector3.Distance(this.root.position,target),spawnDistance=Math.hypot(this.root.position.x-spawn.x,this.root.position.z-spawn.z);if((this.aiState==='IDLE'||this.aiState==='RETURNING')&&playerDistance<config.detectionRadius){this.aiState='CHASING';this.wanderTarget=undefined;}else if(spawnDistance>config.leashRadius&&playerDistance>=config.detectionRadius)this.aiState='RETURNING';else if(this.aiState==='CHASING'&&playerDistance<=config.attackRadius)this.aiState='ATTACKING';else if(this.aiState==='ATTACKING'&&playerDistance>config.attackRadius)this.aiState='CHASING';else if((this.aiState==='CHASING'||this.aiState==='ATTACKING')&&playerDistance>config.detectionRadius*1.45)this.aiState='RETURNING';if(this.aiState==='IDLE')this.updateWander(spawn,config.movementSpeed,dt);if(this.aiState==='CHASING')this.moveToward(target.x,target.z,config.movementSpeed,dt);if(this.aiState==='RETURNING'){this.wanderTarget=undefined;this.moveToward(spawn.x,spawn.z,config.movementSpeed,dt);if(spawnDistance<.15){this.aiState='IDLE';this.scheduleWanderPause();}}if(this.aiState==='ATTACKING'&&this.cooldown===0){damagePlayer(config.damage);this.cooldown=config.attackCooldownMs;}this.finishUpdate(dt);}
private updateWander(spawn:Vec2,speed:number,dt:number){if(!this.wanderTarget&&this.wanderWaitMs===0)this.wanderTarget=this.navigation?.findNearbyWalkable(spawn,GAME_CONFIG.monsterWander.radius,GAME_CONFIG.monsterWander.maxTargetAttempts)??undefined;if(!this.wanderTarget)return;this.moveToward(this.wanderTarget.x,this.wanderTarget.z,speed*GAME_CONFIG.monsterWander.speedMultiplier,dt);if(Math.hypot(this.root.position.x-this.wanderTarget.x,this.root.position.z-this.wanderTarget.z)<.2){this.wanderTarget=undefined;this.scheduleWanderPause();}}
private scheduleWanderPause(){const config=GAME_CONFIG.monsterWander;this.wanderWaitMs=config.pauseMinMs+Math.random()*(config.pauseMaxMs-config.pauseMinMs);}private finishUpdate(dt:number){this.state.position={x:this.root.position.x,z:this.root.position.z};this.animateMovement(dt);}setNavigation(navigation:MonsterNavigationSystem){this.navigation=navigation;}
private moveToward(x:number,z:number,speed:number,dt:number){const waypoint=this.navigation?this.navigation.getNextWaypoint(this.state.id,{x:this.root.position.x,z:this.root.position.z},{x,z}):{x,z};if(!waypoint)return;const dx=waypoint.x-this.root.position.x,dz=waypoint.z-this.root.position.z,length=Math.hypot(dx,dz)||1;this.root.position.x+=dx/length*speed*dt;this.root.position.z+=dz/length*speed*dt;this.root.rotation.y=Math.atan2(dx,dz);this.moving=true;}
private animateMovement(dt:number){this.animationTime+=dt;const attacking=this.aiState==='ATTACKING';if(this.state.type==='crawler'){const pace=this.moving?8:2;this.visual.position.y=Math.abs(Math.sin(this.animationTime*pace))*(this.moving?.09:.018);this.visual.rotation.z=Math.sin(this.animationTime*pace)*(this.moving?.09:.025);const lunge=attacking?1+Math.sin(this.animationTime*12)*.1:1;this.visual.scaling.z=lunge;}else{const pace=this.moving?3.2:1.4;this.visual.rotation.z=Math.sin(this.animationTime*pace)*(this.moving?.075:.025);this.visual.position.y=Math.sin(this.animationTime*pace)*.035;const distortion=attacking?1+Math.sin(this.animationTime*10)*.055:1;this.visual.scaling.x=distortion;this.visual.scaling.z=distortion;}}
returnHome(){if(this.state.alive){this.aiState='RETURNING';this.wanderTarget=undefined;}}damage(amount:number){if(!this.state.alive)return false;this.state.health=Math.max(0,this.state.health-amount);this.meshes.forEach(mesh=>{const previous=mesh.scaling.clone();mesh.scaling=previous.scale(.85);setTimeout(()=>mesh.scaling=previous,100);});if(this.state.health===0){this.state.alive=false;this.aiState='DEAD';this.deathTime=.45;return true;}return false;}animateDeath(dt:number){if(this.deathTime<=0)return;this.deathTime-=dt;this.root.scaling.scaleInPlace(Math.max(0,1-dt*3));this.root.rotation.y+=dt*8;if(this.deathTime<=0){this.root.setEnabled(false);this.readyForRemoval=true;}}dispose(){this.navigation?.clear(this.state.id);this.root.dispose(false,true);}}
