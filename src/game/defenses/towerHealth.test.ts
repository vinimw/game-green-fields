import { NullEngine,Scene,TransformNode } from '@babylonjs/core';import { describe,expect,it } from 'vitest';import { DEFENSE_CONFIG } from '../config/defenseConfig';import { createPlayerState } from '../core/GameState';import type { DefenseState } from '../core/types';import { Monster } from '../entities/Monster';import { Player } from '../entities/Player';import { MiniTower } from './MiniTower';
const towerState=():DefenseState=>({id:'tower-health-test',type:'mini-tower',level:1,position:{x:6,y:0,z:0},rotation:0,currentHealth:DEFENSE_CONFIG.miniTower.maxHealth});
describe('Mini Tower health',()=>{it('has 300 maximum health and updates current health',()=>{const engine=new NullEngine(),scene=new Scene(engine),tower=new MiniTower(scene,towerState());expect(DEFENSE_CONFIG.miniTower.maxHealth).toBe(300);expect(tower.damage(22)).toBe(22);expect(tower.state.currentHealth).toBe(278);const healthBar=scene.getTransformNodeByName('tower-health-test-health-bar'),healthFill=scene.getMeshByName('tower-health-test-health-fill');expect(healthBar?.position.y).toBeGreaterThan(3);expect(healthBar?.billboardMode).toBe(TransformNode.BILLBOARDMODE_ALL);expect(healthFill?.isPickable).toBe(false);tower.dispose();scene.dispose();engine.dispose();});});
describe('monster Defense targeting',()=>{it('attacks a tower when outside a Core raid and inside tower range',()=>{const engine=new NullEngine(),scene=new Scene(engine),player=new Player(scene,createPlayerState('archer')),monster=new Monster(scene,{id:'crawler-defense-test',type:'crawler',alive:true,health:16,position:{x:6.5,z:0},spawnPosition:{x:6.5,z:0}});let towerHealth=300,playerDamage=0;monster.update(player,.016,damage=>playerDamage+=damage,false,undefined,[{id:'tower',position:{x:6,z:0},damage:amount=>{towerHealth-=amount;return amount;}}]);expect(towerHealth).toBe(278);expect(playerDamage).toBe(0);monster.dispose();scene.dispose();engine.dispose();});it('prioritizes the Core during an active raid',()=>{const engine=new NullEngine(),scene=new Scene(engine),player=new Player(scene,createPlayerState('archer')),monster=new Monster(scene,{id:'crawler-raid-test',type:'crawler',alive:true,health:16,position:{x:.5,z:0},spawnPosition:{x:.5,z:0}});let towerHealth=300,coreDamage=0;monster.update(player,.016,()=>{},false,{active:true,position:{x:0,z:0},damageBase:amount=>coreDamage+=amount},[{id:'tower',position:{x:.5,z:0},damage:amount=>{towerHealth-=amount;return amount;}}]);expect(coreDamage).toBe(22);expect(towerHealth).toBe(300);monster.dispose();scene.dispose();engine.dispose();});});
