import type { InputSystem } from '../systems/InputSystem';
export class VirtualJoystick {constructor(root:HTMLElement,input:InputSystem){const base=document.createElement('div'),knob=document.createElement('div');base.className='joystick';knob.className='joystick-knob';base.append(knob);root.append(base);const reset=()=>{input.joystick={x:0,z:0};knob.style.transform='translate(-50%,-50%)';};const move=(e:PointerEvent)=>{const r=base.getBoundingClientRect(),x=e.clientX-(r.left+r.width/2),y=e.clientY-(r.top+r.height/2),l=Math.hypot(x,y),max=r.width*.34,k=Math.min(1,max/(l||1));input.joystick={x:x/max*k,z:y/max*k};knob.style.transform=`translate(calc(-50% + ${x*k}px),calc(-50% + ${y*k}px))`;};base.addEventListener('pointerdown',e=>{base.setPointerCapture(e.pointerId);move(e)});base.addEventListener('pointermove',e=>{if(base.hasPointerCapture(e.pointerId))move(e)});base.addEventListener('pointerup',reset);base.addEventListener('pointercancel',reset);}}
